spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${POSTGRES_DB:casino}
    username: ${POSTGRES_USER:casino}
    password: ${POSTGRES_PASSWORD:change-me}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 900000
#logging:
#  level:
#    org.springframework.security: TRACE